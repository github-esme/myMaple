var status = -1;

function start(mode, type, selection) {
	qm.sendNext("You grew up to be so strong!");
	qm.gainExp(500);
	qm.forceCompleteQuest();
	qm.dispose();
}
function end(mode, type, selection) {
	qm.sendNext("You grew up to be so strong!");
	qm.gainExp(500);
	qm.forceCompleteQuest();
	qm.dispose();
}
